{# @pebvariable name="protocol" type="java.lang.String" #}
{# @pebvariable name="host" type="java.lang.String" #}
{# @pebvariable name="token" type="java.lang.String" #}
Reset your Jakon password

Use the link below to set up new password. If you did not request to reset your password, ignore this email.

<a href="{{ protocol | default("http") }}://{{ host }}/resetPasswordStep2?token={{ token }}">{{ protocol | default("http") }}://{{ host }}/resetPasswordStep2?token={{ token }}</a>