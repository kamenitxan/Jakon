package webui

import cz.kamenitxan.jakon.core.configuration.Settings
import org.scalatest.FunSuite
import org.scalatest.selenium.{Firefox, HtmlUnit}
/*
class MenuTest extends FunSuite with Firefox {
	val host = "http://localhost:"  + Settings.getPort + "/admin/"

	test("menu items") {
		go to (host + "index")

		val menuLinks = cssSelector(".sidebar-nav li a")

		println(menuLinks)
	}
}
*/